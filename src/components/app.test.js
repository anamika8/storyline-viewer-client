import React from "react";
import { shallow, mount } from "enzyme";
import App from "./app";

it("renders without crashing", () => {
  it("Renders without crashing", () => {
    shallow(<App />);
  });
});
